# 🚀 راه‌انداز و به‌روزرسان Cloudflare Worker + D1

ابزار وب برای ساخت سریع و بروزرسانی **Cloudflare Worker** و دیتابیس **D1** — بدون کپی‌پیست، بدون نصب wrangler، مناسب موبایل.

> ## ⚡ استفاده فوری — بدون نصب (GitHub Pages)
>
> پنل به صورت **سایت استاتیک** روی GitHub Pages بالا آمده:
> **https://alireza123456w6w.github.io/simple_cloudfire_worker_changer/**
>
> ۱. صفحه را باز کن → دکمه **«نصب پل»** را بزن (فقط یک بار)
> ۲. Token و Account ID را بده، دستور آماده را کپی کن و در Termux/کامپیوتر اجرا کن
> ۳. آدرس پل چاپ‌شده را در صفحه بگذار — تمام! از این به بعد همه‌چیز با موبایل و چند کلیک.
>
> **چرا پل؟** مرورگر اجازه ندارد مستقیم به `api.cloudflare.com` وصل شود (CORS کلادفلر بسته است — تست ۲۰۲۶/۰۹). پل یک ورکر کوچک در خود اکانت توست که فقط درخواست‌ها را رد می‌کند و هیچ چیزی ذخیره نمی‌کند؛ کدش داخل همان صفحه شفاف قابل مشاهده است.
> کد `worker.js` و `setup.sql` همین ریپو هم از تب پنل قابل دریافت و دیپلوی مستقیم است — یعنی: در گیت‌هاب کد را ویرایش کن، در پنل «دریافت از ریپو» + «بروزرسانی» بزن.

برای هر کسی که می‌خواهد چند کلیکی یک Worker + دیتابیس D1 با اسکیمای آماده بالا بیاورد (ربات، API کوچک، وب‌هوک، خودکارسازی…) و بعداً کد یا اسکیمایش را راحت عوض کند.

---

## ✨ چه کاری می‌کند؟

### 🚀 حالت «راه‌اندازی جدید»
فقط سه نام انتخاب کن — نام **Worker**، نام **دیتابیس D1** و نام **Binding** (مثلاً `DB`) — بقیه‌اش با این ابزار:

1. دیتابیس D1 ساخته می‌شود (اگر از قبل باشد، همان استفاده می‌شود — اجرای تکراری امن است)
2. فایل **schema.sql** آپلودی اتوماتیک روی دیتابیس اجرا می‌شود
3. کد **worker.js** آپلود می‌شود و D1 با نام Binding دلخواه به آن وصل می‌شود
4. **Variableها و Secretها** (مثل `BOT_TOKEN`) همان‌جا ست می‌شوند
5. آدرس **workers.dev** فعال و نمایش داده می‌شود (+ قالب آماده setWebhook برای ربات‌ها)

### ♻️ حالت «بروزرسانی»
Worker و دیتابیس **موجود** را از فهرست انتخاب کن و فقط چیزهایی را که می‌خواهی عوض کن:

- **کد جدید** Worker (جایگزین کامل کد قبلی)
- **اسکیمای جدید** روی دیتابیس (برای جدول/ستون‌های جدید — اجرای تکراری امن)
- **افزودن/تغییر Variable و Secret** — با `keep_bindings` مقادیر قبلی دست‌نخورده می‌مانند
- تغییر متغیرها **بدون تغییر کد** (PATCH تنظیمات)
- نمایش وضعیت فعلی Worker: compatibility_date + همه bindingها قبل از تغییر

---

## 🧰 امکانات

- 🔌 اتصال فقط با API Token — اکانت‌ها خودکار شمرده می‌شوند (یا Account ID دستی)
- 📱 آپلود فایل از گوشی، پیست کد، یا استفاده از `worker.js` همین ریپو
- 🔒 مدیریت Variable/Secret با یک دکمه (تغییر نوع V/🔒)
- 🧠 اجرای هوشمند SQL: اول یک‌جا؛ اگر خطا داد، دستور‌به‌دستور برای پیدا کردن دقیق دستور مشکل‌دار (فهم تریگر، رشته‌ها و کامنت‌ها)
- 🛡 گارد ایمنی: کد خالی/ناقص هرگز آپلود نمی‌شود تا Worker زنده‌ات خراب نشود
- ✅ گزارش گام‌به‌گام هر عملیات + راستی‌آزمایی bindingها بعد از آپلود
- 🇮🇷 پیام‌های خطای فارسی برای همه خطاهای رایج Cloudflare
- 🔐 امنیت: توکن فقط در حافظه همین تب (sessionStorage) — هیچ‌جا ذخیره یا لاگ نمی‌شود

---

## 🏃 دو راه اجرا

### راه ۱ — سایت آماده GitHub Pages (پیشنهادی — بدون نصب)
مستقیم برو به: **https://alireza123456w6w.github.io/simple_cloudfire_worker_changer/**

فایل‌های آن در پوشه [`docs/`](docs/) همین ریپو هستند (فقط HTML/CSS/JS خالص) — هر تغییر در آن‌ها خودکار روی سایت اعمال می‌شود.

### راه ۲ — پنل Next.js (لوکال یا Vercel)

```bash
npm install
npm run dev        # http://localhost:3000
```

یا روی **Vercel**: این ریپو را Import کن — تنظیمات خاصی نمی‌خواهد (فایل `worker.js` با `outputFileTracingIncludes` همراه توابع سروری بسته‌بندی می‌شود). نسخه Next.js نیازی به پل ندارد (درخواست‌ها سمت سرور می‌روند).

**API Token لازم:** داشبورد Cloudflare → My Profile → API Tokens → Create Token — دسترسی‌ها:

```
Account · Workers Scripts · Edit
Account · D1 · Edit
User · Memberships · Read   (برای شمردن خودکار اکانت‌ها)
```

---

## 📦 فایل‌های آماده داخل ریپو

| فایل | توضیح |
|---|---|
| `worker.js` | نمونه آماده: کد ربات فروشگاه بله/تلگرام (الان خالی است — کد را از نسخه ۷.۳ داخلش بگذار) |
| `setup.sql` | اسکیمای کامل دیتابیس همان ربات (۸ جدول — اجرای تکراری بی‌خطر) |
| `wrangler.jsonc.example` | فقط اگر خواستی روش پیشرفته Workers Builds کلادفلر را بچینی |

---

## 🧩 API داخلی (برای توسعه‌دهنده‌ها)

| مسیر | کار |
|---|---|
| `POST /api/cf/verify` | اعتبارسنجی توکن + لیست اکانت‌ها |
| `POST /api/cf/resources` | لیست Workerها و دیتابیس‌های D1 |
| `POST /api/cf/worker-info` | تنظیمات یک Worker (compat date + bindingها) |
| `POST /api/cf/d1` | ساخت دیتابیس D1 (اگر موجود بود، همان برمی‌گردد) |
| `POST /api/cf/sql` | اجرای اسکریپت SQL روی D1 (یک‌جا / تکه‌ای با گزارش خطا) |
| `POST /api/cf/deploy` | آپلود کد + Variable/Secret + اتصال D1 + workers.dev |
| `GET /api/worker-code` | سرو محتوای `worker.js` همین ریپو |

---

## 🔒 امنیت

- هیچ توکن یا Secret‌ای در این ریپو نیست — و هرگز commit نکن
- توکن API پنل فقط در `sessionStorage` مرورگر توست و با بستن تب پاک می‌شود
- `.env*` در `.gitignore` است

## 🗂 ساختار

```
├── worker.js                    ← نمونه کد ورکر (الان placeholder است)
├── setup.sql                    ← اسکیمای آماده D1
├── wrangler.jsonc.example       ← برای روش پیشرفته اتصال گیت به Cloudflare
├── src/app/page.tsx             ← رابط کاربری (فارسی RTL، موبایل‌پسند)
├── src/app/api/cf/*             ← ۶ مسیر API کار با Cloudflare
├── src/lib/cf.ts                ← کتابخانه مشترک + جداساز SQL
└── ...
```

## 📄 License

See [LICENSE](LICENSE).
